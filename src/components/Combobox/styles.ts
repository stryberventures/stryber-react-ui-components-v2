import { createStyles } from '../Theme';

export default createStyles(() => ({
  content: {
    padding: [8, 0],
    maxHeight: 190,
    overflow: 'auto',
  },
  clearIcon: {
    marginRight: 10,
    zIndex: 99,
  }
}));
