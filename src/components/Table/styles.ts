import { createStyles } from '../Theme';

export default () => createStyles(() => ({
  table: {
    width: '100%',
    borderSpacing: 0,
    borderCollapse: 'collapse',
  },
}), { internalUsage: true });
