import { createStyles } from '../../Theme';


export default createStyles((theme) => ({
  dialogTitle: {
    marginBottom: 24,
    color: theme.colors.text.headline,
  },
}), { internalUsage: true });
