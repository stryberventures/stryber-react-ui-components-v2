import { createStyles } from '../../Theme';

export default createStyles(
  (theme) => ({
    dialogContentText: {
      lineHeight: 1.2,
      color: theme.colors.text.secondary,
    },
  }),
  { internalUsage: true }
);
