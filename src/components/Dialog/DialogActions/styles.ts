import { createStyles } from '../../Theme';


export default createStyles(() => ({
  dialogActions: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
    gap: 16,
    marginTop: 48,
  },
}));
