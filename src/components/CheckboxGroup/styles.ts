import { createStyles } from '../Theme';

export default createStyles(() => ({
  checkboxGroup: {
    display: 'flex',
    flexDirection: 'column',
  },
  form: {
    marginLeft: 24,
  },
  childCheckbox: {},
}));
